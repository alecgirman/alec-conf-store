# this file makes this a package, so if I see the text below
# when I begin using it like a package, then it works (debugging code)
# Currently, it doesn't matter if it showss up or not.
print('--------------------------------------------------')
print('init.py called in the init script folder.')
print('--------------------------------------------------')
