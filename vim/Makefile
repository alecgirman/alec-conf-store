#!/usr/bin/cat

# THIS MAKE FILE IS NON OPERATIONAL
# # DO NOT USE
# THE CONTENTS OF THE MAKEFILE 
# ARE DSPLAYED BELOW

MKDIR = mkdir -p
DELFILE = rm -fv
DELDIR = rm -rfv

CONFIGPACK = ~/.vim/pack/config
PLUGINPACK = ~/.vim/pack/pluginpack

config:
	$(MKDIR) ~/.vim/pack/config
	$(MKDIR) ~/.vim/pack/pluginpaco
