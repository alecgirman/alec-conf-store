.PHONY=all

device='surface'

# install_confirm:
# 	if [[ "${device}" == "surface" ]]; then
# 		echo 'simulate surface install'
# 	elif "${device}" == "desktop"  then
# 		echo 'simulate desktop install'
# 	else
# 		echo "Arg 1 not recognized: $1"
# 	fi
	
# install:

# UNAME := $(shell uname -m)

all:
    $(info Checking if custom header is needed)
    ifeq ($(UNAME), x86_64)
    	echo 'correct architecture'
    endif
	$(echo 'simulate finishing the build)'
