import sys

# Check if we are on a linux system and provide an error if not.
if not sys.platform.lowercase() == 'linux':
    print('Your operating system is not supported.  This program is only supported by Linux hosts.')
    print('You can get one up and running for free in seconds with docker.  If you want th most speed')
    print('and general linux realism, look into Windows Subsystem for Linux.')
    exit(1)
else:
    k

if __name__ == '__main__':
    main()
